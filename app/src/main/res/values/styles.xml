<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


  <style name="EventSearchStyle">
    <item name="android:layout_height">@dimen/match_parent</item>
    <item name="android:layout_width">@dimen/match_parent</item>
    <item name="android:background">@drawable/background_search_view</item>
    <item name="android:textSize">@dimen/text_body</item>
    <item name="android:maxLines">1</item>
    <item name="android:drawableLeft">@android:drawable/ic_menu_search</item>
    <item name="android:drawablePadding">@dimen/base_padding_half</item>
    <item name="android:imeOptions">actionSearch</item>
    <item name="android:inputType">text</item>
    <item name="android:textColorHint">@color/white</item>
    <!--  To change cursor color mark textCursorDrawable as null then it takes text color as cursor color-->
    <item name="android:textCursorDrawable">@null</item>
    <item name="android:textColor">@color/white</item>
  </style>

</resources>
